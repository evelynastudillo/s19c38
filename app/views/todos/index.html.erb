<h1>Todos#index</h1>
<p>Find me in app/views/todos/index.html.erb</p>
<% @todos.each do |todo| %>
<%= image_tag todo.photo %>
<%= todo.name %>
<%= link_to "Completado!", todo_completedtasks_path(todo), class: 'btn btn-success', method: :post %>

  <% end %>
